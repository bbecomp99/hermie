[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
retry_files_enabled = False
result_format = yaml
interpreter_python = auto_silent

# Only the prereqs task escalates (apt); it sets become per-task.
[privilege_escalation]
become = False
