---
- name: Deploy and configure the Hermes agent
  hosts: hermes
  gather_facts: true

  roles:
    - hermes
    - dashboard
    - mattermost
    - gateway
    - { role: weather, tags: ['weather'] }
